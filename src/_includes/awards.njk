<section class="stephany__content stephany__awards">
    <div class="container">
        <h2>Awards.</h2>
        {% for copy in content.awards %}
            <p>{{copy | safe}}</p>
        {% endfor %}
    </div>
</section>
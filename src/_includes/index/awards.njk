<section class="stephany__content stephany__awards">
    <div class="container">
        <h2>Awards.</h2>
        {% for copy in index.awards %}
            <p>{{copy | safe}}</p>
        {% endfor %}
    </div>
</section>